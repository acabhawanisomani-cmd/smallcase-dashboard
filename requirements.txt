# Pinned scientific stack — a mutually compatible, pre-numpy-2 set that is
# rock-solid on Python 3.12. Leaving these unpinned lets a fresh rebuild pull
# numpy 2.x + latest pandas, whose binaries are incompatible and segfault
# (signal 11) when pandas builds a DataFrame. Do not loosen without testing.
streamlit>=1.30.0
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
# Pinned to a pre-curl_cffi yfinance. Newer yfinance (>=0.2.52) ships a native
# curl_cffi networking layer that segfaults (signal 11) on Streamlit Cloud.
# 0.2.40 uses plain `requests` — no curl_cffi is installed, so the native
# crash is impossible. Prices may be rate-limited by Yahoo (handled gracefully)
# but the app never crashes.
yfinance==0.2.40
plotly>=5.18.0
pyxirr>=0.9.0
psycopg2-binary>=2.9.9
openpyxl>=3.1.0
requests>=2.31.0
