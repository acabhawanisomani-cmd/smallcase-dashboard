# Pinned scientific stack — a mutually compatible, pre-numpy-2 set that is
# rock-solid on Python 3.12. Leaving these unpinned lets a fresh rebuild pull
# numpy 2.x + latest pandas, whose binaries are incompatible and segfault
# (signal 11) when pandas builds a DataFrame. Do not loosen without testing.
streamlit>=1.30.0
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
# yfinance removed entirely — it pulls native C-extensions (curl_cffi,
# frozendict) that segfault on Streamlit Cloud. Prices are now fetched with
# plain `requests` against Yahoo's public JSON endpoint (see finance.py).
plotly>=5.18.0
pyxirr>=0.9.0
psycopg2-binary>=2.9.9
openpyxl>=3.1.0
# `requests` also powers the news feed (Google News RSS, parsed with the
# stdlib xml module — deliberately no feedparser/lxml, see finance.py).
requests>=2.32.0
# NOTE: touch this file whenever database.py/finance.py gain new functions.
# Streamlit Cloud otherwise serves a cached module and every call to the new
# helper fails with AttributeError until the environment is rebuilt.
# Rebuild marker: clients+client_holdings tables (advisory clients)
